import { LuBuilding, LuSettings } from 'react-icons/lu';

export enum Icons {
  Variables = 'Variables',
  Rows = 'Rows',
}

export const ICONS = {
  [Icons.Variables]: LuSettings,
  [Icons.Rows]: LuBuilding,
};
